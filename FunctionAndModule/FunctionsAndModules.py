# this file contain the function and modules examples
import  math
#here own package U have created and imported the modules.
from  mypacakge import  module1

print(math.sqrt(9))
print(math.isqrt(9))

module1.hello()
print(module1.getValue())